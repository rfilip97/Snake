package SnakeSRC;

public class GameConfig {
	static final int FRAME_SPEED = 100; // one frame is drawn each 'FRAME_SPEED' ms
	static final int HEIGHT = 800;
	static final int WIDTH = HEIGHT;
	static final int UNIT = HEIGHT / 100;
	static final int NR_OF_UNITS_IN_LINE = WIDTH / UNIT;
}
